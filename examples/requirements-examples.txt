# cline_rag examples -- 예제 전용 선택 의존성
#
# 메인 앱(requirements.txt / requirements-optional.txt)과 완전히 분리된,
# examples/ 폴더의 학습용 예제를 돌릴 때만 필요한 패키지입니다.
#
# 설치:
#   python -m pip install -r examples/requirements-examples.txt

# ---------------------------------------------------------------------------
# llama.cpp (01_llama_cpp, 02_langchain/05, 03_langgraph/04)
# ---------------------------------------------------------------------------
# 인프로세스(GGUF 직접 로드) 예제에서 사용. provider="llama_cpp" 와 동일한 의존성.
llama-cpp-python>=0.3.0

# ---------------------------------------------------------------------------
# LangSmith 트레이싱/평가 (04_langsmith)
# ---------------------------------------------------------------------------
# langchain/langgraph 실행을 클라우드에 추적하거나 데이터셋을 만들 때 사용.
langsmith>=0.13.0

# ---------------------------------------------------------------------------
# .env 로딩 (04_langsmith 에서 워크스페이스 루트의 .env 를 자동 로드)
# ---------------------------------------------------------------------------
python-dotenv>=1.2.0
