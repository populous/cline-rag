[pytest]
# 테스트 탐색 범위
testpaths = tests

# src(소스)와 tests(conftest) 를 임포트 경로에 넣는다.
# CTest 는 작업 디렉토리를 바꿀 수 있으므로 이 설정이 필요하다.
pythonpath = src tests

# 실패 시 자세히 보여주고, 경고 요약도 출력
addopts = -ra --strict-markers

# 테스트 이름 규칙(기본값과 동일하지만 명시)
python_files = test_*.py
python_functions = test_*