# cline_rag -- 선택적 확장 의존성
#
# 기본 동작에는 전혀 필요하지 않습니다. RAG_STEP_BY_STEP.md 11장의
# 확장 기능을 실제로 쓸 때만 설치하세요.
#
# 참고: v2.0.0부터 벡터 저장소가 Chroma(langchain-chroma)로 바뀌면서
# numpy는 chromadb의 필수 의존성으로 자동 설치됩니다. 더 이상 별도로
# 선택 설치할 필요가 없어 여기서는 제거했습니다.

# ---------------------------------------------------------------------------
# PDF 문서 색인
# ---------------------------------------------------------------------------
# docs/ 에 PDF 를 넣고 싶을 때 필요합니다. LangChain의 PyPDFLoader가 사용합니다.
pypdf>=5.0

# ---------------------------------------------------------------------------
# llama.cpp 임베딩 provider (config.json 의 embedding.provider 를 바꿔 사용)
# ---------------------------------------------------------------------------
# provider="llama_cpp" (인프로세스, GGUF 직접 로드) 를 쓸 때만 필요합니다.
# provider="llama_cpp_server" 는 기존 langchain-openai 를 재사용하므로
# 추가 의존성이 없습니다. 상세는 docs/llama_cpp_provider.md 참고.
llama-cpp-python>=0.3.0
