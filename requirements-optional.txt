# cline_rag -- 선택적 확장 의존성
#
# 기본 동작에는 전혀 필요하지 않습니다. RAG_STEP_BY_STEP.md 11장의
# 확장 기능을 실제로 쓸 때만 설치하세요.

# ---------------------------------------------------------------------------
# 검색 속도 향상
# ---------------------------------------------------------------------------
# 청크가 수만 개를 넘으면 순수 파이썬 코사인 계산이 느려집니다.
# numpy 로 벡터를 행렬화하면 10~50배 빨라집니다.
# (현재 rag_core.cosine() 은 numpy 없이도 동작합니다)
numpy>=2.0

# ---------------------------------------------------------------------------
# PDF 문서 색인
# ---------------------------------------------------------------------------
# docs/ 에 PDF 를 넣고 싶을 때 필요합니다.
# ingest 전에 텍스트를 추출하는 단계가 필요합니다(11장 참고).
pypdf>=5.0
