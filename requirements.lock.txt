# cline_rag -- 잠금 파일 (pip freeze 기록)
#
# 생성 방법:
#   .\.venv\Scripts\Activate.ps1
#   python -m pip freeze > requirements.lock.txt
#
# 이 프로젝트는 필수 서드파티 의존성이 0개이므로(표준 라이브러리만 사용)
# 아래 실제 기록은 비어 있습니다. 이는 누락이 아니라 설계입니다.
# 자세한 내용은 requirements.txt 주석 참고.
#
