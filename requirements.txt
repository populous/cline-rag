# cline_rag -- 런타임 의존성
#
# 이 프로젝트는 표준 라이브러리만 사용하므로 필수 서드파티 패키지가 없습니다.
# 그래서 아래 "필수" 항목은 비어 있습니다. 이는 누락이 아니라 설계입니다.
#
#   * 이유: Python 3.14 환경에서 pip 설치 없이 바로 동작하도록 만들었습니다.
#   * 검증: python -c "import ast,sys,pathlib; ..." 로 서드파티 임포트 0건 확인
#   * 사용 중인 표준 라이브러리:
#       argparse, json, math, os, pathlib, sqlite3,
#       subprocess, sys, traceback, typing, urllib
#
# MCP 서버(rag_server.py)는 별도 웹 프레임워크 없이 stdio JSON-RPC 를
# 직접 구현했고, 벡터 저장소는 sqlite3, 유사도 계산은 순수 파이썬으로 처리합니다.
#
# ---------------------------------------------------------------------------
# 필수 (Required)
# ---------------------------------------------------------------------------
# (없음)


# ---------------------------------------------------------------------------
# 선택 (Optional) -- 필요할 때만 설치하세요
# ---------------------------------------------------------------------------
# 자세한 설명은 requirements-optional.txt 참고
#   pip install -r requirements-optional.txt


# ---------------------------------------------------------------------------
# 설치 방법
# ---------------------------------------------------------------------------
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt          # 의존성 없음(검증용)
#
# 설치 후 동작 확인:
#   python ingest.py --stats
#   python test_mcp.py
