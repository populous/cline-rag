# cline_rag -- 런타임 의존성
#
# v2.0.0부터 LangChain + LangGraph 기반으로 전환되었습니다.
# (이전 버전은 표준 라이브러리만으로 구현되어 있었습니다.)
#
#   * 임베딩       : langchain-ollama / langchain-openai
#   * 벡터 저장소  : langchain-chroma (chromadb, 로컬 디스크 영속)
#   * 청킹         : langchain-text-splitters
#   * 키워드 검색  : langchain-community의 BM25Retriever (+ rank_bm25)
#   * 검색 오케스트레이션 : langgraph (StateGraph 로 vector/keyword/hybrid 분기)
#
# MCP 서버(rag_server.py) 자체는 여전히 stdio JSON-RPC 를 직접 구현합니다
# (별도 웹 프레임워크 불필요).
#
# ---------------------------------------------------------------------------
# 필수 (Required)
# ---------------------------------------------------------------------------
langchain-core>=1.0
langchain-text-splitters>=1.0
langchain-chroma>=1.0
chromadb>=1.0
langchain-ollama>=1.0
langchain-openai>=1.0
langchain-community>=0.4
rank_bm25>=0.2
langgraph>=1.0

# ---------------------------------------------------------------------------
# 선택 (Optional) -- 필요할 때만 설치하세요
# ---------------------------------------------------------------------------
# 자세한 설명은 requirements-optional.txt 참고
#   pip install -r requirements-optional.txt


# ---------------------------------------------------------------------------
# 설치 방법
# ---------------------------------------------------------------------------
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt          # 런타임 의존성
#   python -m pip install -r requirements-dev.txt      # 테스트(pytest)
#
# 설치 후 동작 확인:
#   python src\ingest.py --stats
#   python smoke_mcp.py
